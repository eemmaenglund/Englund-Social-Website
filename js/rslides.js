// Applying the responsiveSlides function to elements with class "rslides" when the DOM is fully loaded
$(function(){
    // Initialize responsive slides for elements with class "rslides"
    $(".rslides").responsiveSlides();
});